# table - 表格处理模块

## 🎯 一句话定位

提供**报告表格的配置解析、数据转换、渲染器定义**，支持横纵表转换和多种自定义渲染逻辑。

## 📁 目录结构

```
src/table/
├── cell/                    # 单元格处理
│   ├── custom/              # 自定义渲染器（行业、评级等特殊字段）
│   ├── renderers/           # 通用渲染器（货币、日期等）
│   ├── index.ts             # 导出
│   ├── shared.ts            # 共享工具
│   └── type.ts              # 类型定义
├── comment.ts               # 表格注释处理
├── config.ts                # 表格默认配置
├── parser.ts                # 表格解析逻辑
└── index.ts                 # 统一导出
```

## 🚀 核心功能

### 表格配置与解析
| 功能 | 说明 | 位置 |
| :--- | :--- | :--- |
| **`parseTableConfig`** | 解析表格配置 | `parser.ts` |
| **`instanceTableDefaultOptions`** | 默认表格配置 | `config.ts` |
| **`tableIndexColumn`** | 序号列配置 | `config.ts` |

### 单元格渲染器
| 类型 | 渲染器 | 说明 |
| :--- | :--- | :--- |
| **通用渲染器** | `currencyRenderers` | 货币金额格式化 |
|  | `dateRenderers` | 日期时间格式化 |
|  | `numberRender` | 数值格式化 |
| **自定义渲染器** | `creditCodeRenderers` | 统一信用代码 |
|  | `industryGbFold` | 行业分类折叠显示 |
|  | `bondIssueRating` | 债券发行评级 |

## 💡 使用示例

```typescript
import {
  parseTableConfig,
  tableIndexColumn,
  currencyRenderers
} from 'report-util/table';

// 解析表格配置
const config = parseTableConfig(rawConfig);

// 添加序号列
const columns = [tableIndexColumn, ...config.columns];

// 使用货币渲染器
const currencyCol = {
  title: '金额',
  dataIndex: 'amount',
  render: currencyRenderers.yuan
};
```

## 🎨 渲染器特性

- **国际化支持**: 通过 `t()` 函数支持多语言
- **数据容错**: 空值显示为 `'--'`
- **格式统一**: 预览与打印使用相同渲染逻辑
- **扩展性**: 易于添加新的自定义渲染器

## ⚠️ 注意事项

- **wkhtmltopdf兼容**: 渲染器代码必须ES5兼容
- **性能考虑**: 避免复杂渲染逻辑影响PDF生成
- **数据安全**: 所有用户输入需通过 `replaceScript` 清理

## 📖 相关文档

- [report-util 主文档](../README.md)
- [misc模块 - replaceScript](../misc/README.md)