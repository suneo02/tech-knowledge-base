---
description: Description: TypeScript coding standards for the project
globs: **/*.{ts,tsx}
---

- 所有的 Props 接口必须以 Props 结尾，例如 `ButtonProps`
- 使用 type 而不是 interface，除非你需要声明可以被扩展的接口
- 优先使用 const enum 而不是普通 enum
- 总是显式声明函数返回类型
- 使用 React.FC<Props> 类型来声明函数组件

@file tsconfig.json