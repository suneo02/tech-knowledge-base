/**
 * Main entry point for detail-page-config
 *
 * NOTE: Import specific modules directly to benefit from tree-shaking:
 * import { creditReportConfig } from 'detail-page-config/corp'
 * import { ICfgDetailJSON } from 'detail-page-config/types'
 * import { validateBusinessRiskConfig } from 'detail-page-config/validation'
 */

// Re-export specific modules for convenience
export * from './corp'
export * from './validation'
