import { ReportDetailNodeWithChildrenJson } from 'gel-types'

/**
 * 验证 ReportDetailNodeWithChildrenJson 实例
 *
 * TODO
 * @param instance 要验证的配置实例
 * @returns 验证结果
 */
export function validateReportDetailNodeWithChildrenJson(instance: any): ReportDetailNodeWithChildrenJson {
  return instance
}
