## 目标
- 彻底删除“上传文件后自动提示是否生成全文”的代码实现，不通过参数控制。

## 删除内容
1. 删除上传提示弹窗与相关回调
- 文件：`apps/report-ai/src/components/Reference/ReportFileUpload/index.tsx`
- 删除内容：
  - `Popover` 弹窗与其可见状态、文案与按钮（约 `50-53, 65-71, 74-84, 85-104, 116-126`）。
  - 属性 `onRegenerationConfirm` 与 `showRegenerationPrompt` 及其使用（`19-28, 42-47, 75-79`）。
  - 移除不再使用的 `Button, Popover` 引入（`15`）。
- 保留：`UploadFileBtn` 上传按钮与 `onUploadSuccess` 回调（列表刷新逻辑继续生效）。

2. 移除上层传参与类型声明
- 文件：`apps/report-ai/src/components/Reference/ReferenceView/index.tsx`
- 删除内容：
  - `RPReferenceViewProps` 中的 `onRegenerationConfirm`、`showRegenerationPrompt`（`41-49`）。
  - 传递到 `ReportFileUpload` 的上述两个属性（`127-133` 仅保留 `reportId`、`onUploadSuccess`）。

3. 移除右侧面板的传参与依赖
- 文件：`apps/report-ai/src/pages/ReportDetail/RightPanel/index.tsx`
- 删除内容：
  - 去掉 `onRegenerationConfirm={startGeneration}` 传参（`158-161`）。
  - 其他逻辑（文件刷新、解析轮询、章节再生成）保持不变。

## 验证
- 上传文件后：仅刷新文件列表与继续解析轮询，不再出现任何生成全文提示。
- 页头“生成全文”按钮仍可正常触发：`apps/report-ai/src/pages/ReportDetail/ReportContent/index.tsx:213-217`。
- TypeScript 编译通过，无未使用的变量与类型错误。

## 影响范围与回滚
- 该行为仅在“报告详情”上传入口生效，删除为彻底移除逻辑，不影响删除文件后的章节再生成弹窗（该功能保持原状）。
- 如需回滚，恢复被删除的属性与弹窗实现即可。