---
description: 
globs: 
---
# 方法文档模板

## 一、基本信息

### 文件信息

```typescript
文件路径：[填写文件路径]
更新时间：[YYYY-MM-DD]
作者：[作者名称]
版本号：[版本号，如：1.0.0]
```

### 方法概述

```typescript
方法名称：[方法名]
功能描述：[一句话描述该方法的主要功能]
应用场景：[详细描述该方法适用的具体场景]
解决问题：[列举该方法解决的具体业务问题]
```

## 二、详细说明

### 类型定义

```typescript
// 入参类型定义
interface RequestParams {
  // 在此处定义入参类型
}

// 返回值类型定义
interface ResponseData {
  // 在此处定义返回值类型
}

// 其他辅助类型定义（如果需要）
```

### 方法签名

```typescript
const methodName = (params: RequestParams): ResponseData => {
  // 方法实现
}
```

## 三、使用示例

### 基础用法

```typescript
// 在此处提供基础调用示例
```

### 完整示例

```typescript
// 在此处提供包含错误处理的完整示例
```

### 常见场景

```typescript
// 在此处提供常见使用场景示例
```

## 四、实现细节

### 业务流程

```typescript
/**
 * 1. [步骤1]
 * 2. [步骤2]
 * 3. [步骤3]
 * ...
 */
```

### 依赖说明

```typescript
/**
 * 外部依赖：
 * 1. [依赖1]：[用途说明]
 * 2. [依赖2]：[用途说明]
 * ...
 */
```

### 核心逻辑

```typescript
/**
 * [在此处说明核心算法或处理逻辑]
 */
```

## 五、注意事项

### 使用限制

```typescript
/**
 * 1. 性能考虑
 *    - 时间复杂度：
 *    - 空间复杂度：
 *    - 并发限制：
 *
 * 2. 资源消耗
 *    - 内存使用：
 *    - CPU使用：
 *    - 网络请求：
 */
```

### 错误处理

```typescript
/**
 * 可能的错误类型：
 * 1. [错误类型1]
 *    - 错误码：
 *    - 原因：
 *    - 处理建议：
 *
 * 2. [错误类型2]
 *    - 错误码：
 *    - 原因：
 *    - 处理建议：
 */
```

## 六、测试用例

```typescript
describe('[方法名]', () => {
  // 正常场景测试
  test('should handle normal case', () => {
    // 测试代码
  })

  // 异常场景测试
  test('should handle error case', () => {
    // 测试代码
  })

  // 边界条件测试
  test('should handle boundary case', () => {
    // 测试代码
  })
})
```

## 七、更新日志

```typescript
/**
 * v1.0.0 (YYYY-MM-DD)
 * - [变更内容]
 * - [变更原因]
 */
```

## 八、相关资源

```typescript
/**
 * 相关文档：
 * 1. [文档名称]：[链接]
 *
 * 参考资料：
 * 1. [资料名称]：[链接]
 */
```
